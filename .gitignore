# build output
dist

# dependencies
node_modules/

# logs
npm-debug.log*

# environment variables
.env
.env.local
.env.production

# macOS-specific files
.DS_Store

# IDE settings
.vscode
.idea

# package lock
package-lock.json

# astro build files
.astro

# Local composer content is initialized by init-source.mjs.
# Keep source/ visible to Keystatic local storage (it respects .gitignore).
# If this project is in a Git repo, init-source.mjs adds source/ to
# .git/info/exclude instead of .gitignore.

# Generated manifests
.ingest-manifest.json
.deploy-manifest.json

# Private PDF scores (generated at build time)
private/

# Starter kit output (if re-generating)
.starter-kit/
